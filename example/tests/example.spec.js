var expect = require('expect.js');

describe('Example test', function() {
	beforeEach(function() {

	});

	afterEach(function() {

	});

	it('Should be an example test', function() {
		expect(1).to.be(1);
	});
});